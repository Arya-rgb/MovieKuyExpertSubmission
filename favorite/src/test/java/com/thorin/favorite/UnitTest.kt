package com.thorin.favorite

import org.junit.Assert
import org.junit.Test

//untuk cek apakah unit test berjalan di circle ci

class UnitTest {
    @Test
    fun addition_isCorrect() {
        Assert.assertEquals(4, 2 + 2)
    }
}